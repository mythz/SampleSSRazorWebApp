@{
    var sess = GetSession<APSession>();

    bool activeSession = (null != sess && null != sess.default_asp);
}
<nav class="navbar navbar-default navbar-inverse navbar-static-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
  @if (activeSession)
  {
    <a href="/optimize/campaigns" class="navbar-brand" >
      <img src="/images/@sess.default_asp.logo()" width="140" alt="Audience Point" /> Send time optimization</a>
  }
  else
  {
    <a class="navbar-brand" href="#"> Send Time Optimization</a>
  }
 
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse-primary">
          <span class="sr-only">Toggle Side Navigation</span>
          <i class="icon-th-list"></i>
        </button>

        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse-top">
          <span class="sr-only">Toggle Top Navigation</span>
          <i class="icon-align-justify"></i>
        </button>
  </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse navbar-collapse-top">
        <div class="navbar-right">

        @if (activeSession)
        { 
          <ul class="nav navbar-nav navbar-left">
            <li class="cdrop"> @sess.app_user.Timezone</li>
          </ul>
        }
          <ul class="nav navbar-nav navbar-left">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle dropdown-avatar" data-toggle="dropdown">
              <span> 
                <span>Settings <i class="icon-caret-down"></i></span>
              </span>
              </a>
              <ul class="dropdown-menu">
                <!-- the first element is the one with the big avatar, add a with-image class to it -->
                <li class="with-image">
                  <span>Settings</span>
                </li>
                <li class="divider"></li>
          
              <!--  <li><a href="#"><i class="icon-cog"></i>      <span>Account</span></a></li>
                <li><a href="#"><i class="icon-envelope"></i> <span>Billing</span></a></li> -->

                   @if (activeSession)
                   {
                <li><a href="/optimize/account/edit"><i class="icon-user"></i>      <span>Account</span></a></li>

                <li><a href="/optimize/account/changepassword"><i class="icon-key"></i>      <span>Reset Password</span></a></li>
                   }

                <li><a href="/optimize/auth/logout"><i class="icon-off"></i>      <span>Logout</span></a></li>

                
              </ul>
            </li>
          </ul>
        </div>

      </div><!-- /.navbar-collapse -->
</nav>
